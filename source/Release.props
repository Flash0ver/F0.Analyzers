<Project>

  <PropertyGroup>
    <F0Version>0.7.1</F0Version>
    <F0ReleaseNotes>Fixed null check code fixes to replace null checks with pattern matching in arguments.</F0ReleaseNotes>
  </PropertyGroup>

</Project>
